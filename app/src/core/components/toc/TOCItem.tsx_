import React, { Component } from 'react';
import { observer } from 'mobx-react';
import { Hot } from 'codex_core';
import { Anchor } from 'antd';
import PropTypes from 'prop-types';

const { Link } = Anchor

const log = require('debug')('components:TOCItem')

export interface TOCItemProps {
    href:string
    title:string
}

/**
 * TOCItem component
 */
@Hot(module, false)
export default class TOCItem extends Link {
    static displayName: string                 = 'TOCItem'

}
